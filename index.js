// greet user(string): hello
const greet = (string) => `hello ${string}`;

const add = (a, b) => a + b;

const reverse = (string) => string.reverse();


module.exports = {
    greet,
    add,
    reverse,
}